
  <style>
.page-content{
    padding:5px;
    margin-top:2px;
    font-size: var(--big);
    line-height:calc(var(--big) + 5px);
    background:var(--grey);
}
    .freeshippingmenu{
  z-index:10;
  display:block;
  position:absolute;
  top:-26px;
  right:0;
}
</style>